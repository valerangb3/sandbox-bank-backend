ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080

jwt:
    secret: ${JWT_SECRET}
    issuer: ${JWT_ISSUER}
    audience: ${JWT_AUDIENCE}
    realm: ${JWT_REALM}

#jwt:
#    secret: secret
#    issuer: https://vgb3.com/
#    audience: sandbox-bank
#    realm: Access to sandbox-bank